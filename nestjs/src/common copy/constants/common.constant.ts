export enum VISIBLE_STATE {
  PUBLISH = 'publish',
  DRAFT = 'draft',
  OFF = 'off',
}

export enum LOGIC_TYPE {
  NONE = null,
  TO_WEBVIEW = 'to_webview',
  TO_SCREEN = 'to_screen',
  TO_MENU = 'to_menu',
  TO_SITUATION = 'to_situation',
  TO_BROWSER = 'to_browser',
  RELOAD_DATA = 'reload_data',
  APP_REVIEW = 'app_review',
}

export enum OS_ENUM {
  ALL = 'all',
  IOS = 'IOS',
  ANDROID = 'Android',
}

export enum OS_QUERY {
  IOS = 'IOS',
  ANDROID = 'Android',
}
