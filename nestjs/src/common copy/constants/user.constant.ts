export enum USER_TYPE {
  FREE = 'free',
  SUBSCRIPTION = 'subscription',
}

export enum LOGIN_TYPE {
  APPLE = 'apple',
  GOOGLE = 'google',
}

export enum DEVICE_TYPE {
  IOS = 'ios',
  ANDROID = 'android',
}
